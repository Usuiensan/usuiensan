source "https://rubygems.org"

# If you want to use GitHub Pages, uncomment the next line and
# customize the `github-pages` gem.
gem "github-pages", "~> 232", group: :jekyll_plugins

# If you want to use a theme, uncomment the next line and
# replace "minima" with your theme name.
# gem "minima", "~> THEME_VERSION"

# If you have any plugins that are not part of GitHub Pages,
# uncomment the next line and add them here.
# group :jekyll_plugins do
#   gem "jekyll-feed"
#   gem "jekyll-sitemap"
# end

# Build with `bundle exec jekyll serve`